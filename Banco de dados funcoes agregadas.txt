SELECT SUM(estoque)
FROM MEDICAMENTOS

SELECT AVG(EXTRACT(YEAR FROM CURRENT_DATE) - EXTRACT(YEAR FROM DATA_NASCIMENTO))
FROM TUTOR

SELECT MIN(preco)
FROM MEDICAMENTOS

SELECT MAX(peso)
FROM PACIENTE

SELECT COUNT(*)
FROM TUTOR

SELECT id_medicamento, SUM(preco * estoque)
FROM MEDICAMENTOS
GROUP BY id_medicamento

SELECT i.id_receita, SUM(i.total_item)
FROM ITENSRECEITA i
GROUP BY i.id_receita
HAVING SUM(i.total_item) > 100